PORT=5000

# MySQL Database (use cloud MySQL for production)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=employee_management
DB_PORT=3306
DB_SSL=true

# JWT
JWT_SECRET=change_this_to_a_very_long_random_secret_string_min_32_chars
JWT_EXPIRES_IN=24h

# Frontend URL for CORS (your Netlify URL)
FRONTEND_URL=https://jayamweb.netlify.app
